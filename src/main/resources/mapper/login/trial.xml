<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.scyllacore.dumpWeb.commonModule.db.mapper.login.TrialMapper">

    <!--  로그인 정보 체크  -->
    <select id="selectTrialUserInfo" parameterType="Login" resultType="Login">
        SELECT * FROM tsitepw WHERE userId = #{userId}
    </select>
</mapper>


